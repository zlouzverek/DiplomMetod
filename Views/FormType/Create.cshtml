@using DiplomMetod.Models
@model FormTypeCreateViewModel;


@{
    ViewData["Title"] = "Создать тип формы";
}
<h1>@ViewData["Title"]</h1>

<form asp-action="Create" method="post">
    <div class="row g-3">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
              
        <div class="col-md-6">
            <label asp-for="Name" class="control-label">Номер формы</label>
            <input asp-for="Name" class="form-control" placeholder="Введите номер формы" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>

        <div class="col-md-6">
            <label asp-for="FullName" class="control-label">Полное наименование формы</label>
            <input asp-for="FullName" class="form-control" placeholder="Введите полное наименование формы" />
            <span asp-validation-for="FullName" class="text-danger"></span>
        </div>
    </div>

    <div class="form-group mt-3">
        <input type="submit" value="Создать" class="btn btn-dark me-2" />
    </div>

</form>




<div>
    <a asp-action="Index">К списку форм</a>
</div>







@{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
}

